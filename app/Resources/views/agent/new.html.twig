{% extends 'base.html.twig' %}

{% block container %}
    <h1>Agent creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success" />
    {{ form_end(form) }}


    <a href="{{ path('agent_index') }}" class="buttonPrevious btn btn-primary" >Back to the list</a>
    
{% endblock %}
