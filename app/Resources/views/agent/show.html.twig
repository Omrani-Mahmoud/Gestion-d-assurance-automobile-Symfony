{% extends 'base.html.twig' %}

{% block container %}
    <h1>Agent</h1>

    <table class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr class="column-title">
                <th>Idagent</th>
                <td>{{ agent.id }}</td>
            </tr>
            <tr class="column-title">
                <th>Cin</th>
                <td>{{ agent.cin }}</td>
            </tr>
            <tr class="column-title">
                <th>Nom</th>
                <td>{{ agent.nom }}</td>
            </tr>
            <tr class="column-title">
                <th>Prenom</th>
                <td>{{ agent.prenom }}</td>
            </tr>
            <tr class="column-title">
                <th>Telephone</th>
                <td>{{ agent.telephone }}</td>
            </tr>
            <tr class="column-title">
                <th>Adresse</th>
                <td>{{ agent.adresse }}</td>
            </tr>
            <tr class="column-title">
                <th>Email</th>
                <td>{{ agent.email }}</td>
            </tr>
        </tbody>
    </table>



            <a href="{{ path('agent_index') }}" class="buttonPrevious btn btn-primary">Back to the list</a>

            <a href="{{ path('agent_edit', { 'id': agent.id }) }}"class="btn btn-app"><i class="fa fa-edit"></i>Edit</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}

{% endblock %}
