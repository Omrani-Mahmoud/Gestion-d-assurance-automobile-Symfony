{% extends 'base.html.twig' %}

{% block container %}
    <h1>Vehicule edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    <input type="submit" value="Save" class="btn btn-success">
    {{ form_end(edit_form) }}

            <a href="{{ path('gestion_assure_liste_vehicule') }}" class="buttonPrevious btn btn-primary">Back to the list</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}

{% endblock %}

{% block stylesheets %}
    <!-- bootstrap-datetimepicker -->
    <link href="{{ asset('bootstrap-datetimepicker/css/bootstrap-datetimepicker.min.css') }}" rel="stylesheet">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('bootstrap-datetimepicker/js/bootstrap-datetimepicker.min.js') }}"></script>
    <script>
        $('.js-datepicker').datetimepicker({
            useCurrent: true
        });
    </script>
{% endblock %}
