{% extends 'base.html.twig' %}
{% block container %}
    <h1>Nouveau assure</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-primary"/>
    <a class="btn btn-success" href="{{ path('assure_index') }}">Back to the list</a>
    {{ form_end(form) }}

{% endblock %}
