{% extends 'base.html.twig' %}

{% block container %}
    <h1>Rapport edit</h1>
<div style="position: relative;">

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    <input type="submit" value="Save"  class="btn btn-success"/>
    {{ form_end(edit_form) }}
</div>



            <a href="{{ path('rapport_index') }}" class="buttonPrevious btn btn-primary" >Back to the list</a>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}

{% endblock %}
{% block stylesheets %}
    <!-- bootstrap-datetimepicker -->
    <link href="{{ asset('bootstrap-datetimepicker/css/bootstrap-datetimepicker.min.css') }}" rel="stylesheet">
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('bootstrap-datetimepicker/js/bootstrap-datetimepicker.min.js') }}"></script>
    <script>
        $('.js-datepicker').datetimepicker({
            format: 'Y-M-D'
        });
        $('.js-timepicker').datetimepicker({
            format: 'HH:mm'
        });
    </script>
{% endblock %}
