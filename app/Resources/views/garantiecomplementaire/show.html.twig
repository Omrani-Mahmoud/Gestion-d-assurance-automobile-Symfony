{% extends 'base.html.twig' %}

{% block container %}
    <h1>Garantiecomplementaire</h1>

    <table  class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr>
                <th>Idtype</th>
                <td>{{ garantieComplementaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ garantieComplementaire.nom }}</td>
            </tr>
            <tr>
                <th>Tarif de base</th>
                <td>{{ garantieComplementaire.tarifDeBase }}</td>
            </tr>
            <tr>
                <th>Niv franchise</th>
                <td>{{ garantieComplementaire.nivFranchise }}</td>
            </tr>
            <tr>
                <th>Prime de base</th>
                <td>{{ garantieComplementaire.primeDeBase }}</td>
            </tr>
            <tr>
                <th>Surprime</th>
                <td>{{ garantieComplementaire.surprime }}</td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('gestion_listGarantie') }}" class="buttonPrevious btn btn-primary">Back to the list</a>

            <a href="{{ path('garantiecomplementaire_edit', { 'id': garantieComplementaire.id }) }}" class="btn btn-app"><i class="fa fa-edit"></i>Edit</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}

{% endblock %}
